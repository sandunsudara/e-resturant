export const DEFAULT_THEME_MODE = 'light';

export const BASE_URL = 'https://e-restaurant-api.mobios.lk/api/v1';
export const MEDIA_BASE_URL = 'https://e-restaurant-api.mobios.lk/api/v1/media';

const config = {
  fontFamily: 'Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
  borderRadius: 8,
  outlinedFilled: false,
  palette: {}
};

export default config;
